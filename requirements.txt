# OpenBB Core Platform
openbb>=4.0.0

# OpenBB Extensions - Required for data access
openbb-benzinga>=1.5.0
openbb-bls>=1.2.0
openbb-cftc>=1.2.0
openbb-commodity>=1.4.0
openbb-congress-gov>=1.1.0
openbb-crypto>=1.5.0
openbb-currency>=1.5.0
openbb-derivatives>=1.5.0
openbb-econdb>=1.4.0
openbb-economy>=1.5.0
openbb-equity>=1.5.0
openbb-etf>=1.5.0
openbb-federal-reserve>=1.5.0
openbb-fixedincome>=1.5.0
openbb-fmp>=1.5.1
openbb-fred>=1.5.0
openbb-imf>=1.2.0
openbb-index>=1.5.0
openbb-intrinio>=1.5.0
openbb-news>=1.5.0
openbb-oecd>=1.5.0
openbb-polygon>=1.5.0
openbb-regulators>=1.5.0
openbb-sec>=1.5.0
openbb-tiingo>=1.5.0
openbb-tradingeconomics>=1.5.0
openbb-us-eia>=1.2.0
# Note: openbb-uscongress may not be available as a separate package
# It might be included in the core OpenBB platform
openbb-yfinance>=1.5.0

# Core data science and financial analysis
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0
requests>=2.31.0  # For HTTP requests (more reliable than urllib)
python-dotenv>=1.0.0  # For loading .env files securely
typer>=0.12.3
pytest-html>=4.1.1
pyarrow>=15.0.0
yfinance>=0.2.54
py_vollib>=1.0.1
plotly>=5.22.0
plotext>=5.3.2

# Data visualization (optional but recommended for analysis)
matplotlib>=3.7.0
seaborn>=0.12.0

# Date and time utilities
python-dateutil>=2.8.0

# Web3 / Trading
mnemonic>=0.20           # BIP39 seed phrase generation
eth-account>=0.11.0      # EVM HD wallet derivation and signing
web3>=6.0.0              # EVM JSON-RPC client (read-only wallet auditing)
solana>=0.36.0,<0.37.0    # Solana JSON-RPC client (sync API)
solders>=0.26.0          # Solana public-key and RPC types
hyperliquid-python-sdk>=0.22.0  # Hyperliquid exchange API SDK
cryptography>=42.0.0    # Fernet encryption for wallet vault

ruff>=0.1.0  # Linter and formatter
mcp>=1.0.0              # Model Context Protocol server

# ── Stocks workflow ──────────────────────────────────────────────────────────
httpx>=0.27.0            # ISM + Massive.com REST client
beautifulsoup4>=4.12.0   # ISM press-release HTML parsing
# playwright is optional — only needed when scraping JS-rendered mirrors.
# Install on demand:
#     pip install playwright && python -m playwright install chromium

# X (Twitter) social sentiment — optional; only needed for `nave stocks x-analyze`.
# Requires logged-in X account auth tokens stored in a local SQLite DB.
twscrape>=0.17           # async X search client

